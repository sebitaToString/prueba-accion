on: workflow_dispatch
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Prueba accion
    steps:
      
      - uses: actions/checkout@v3
      - name: Imprimir secretos
        run: |
          echo ${{ secrets.NOMBRE_DB }}
          echo '${{ toJSON(secrets) }}' 
      
      
